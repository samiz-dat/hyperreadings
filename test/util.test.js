/* eslint-env mocha */

// var expect = require('chai').expect
// var utils = require('../lib/utils')

describe('isRdfLiteral', () => {
  it('needs to be tested')
})
describe('fromRdfValue', () => {
  it('needs to be tested')
})
describe('isNode', () => {
  it('needs to be tested')
})
describe('toRdfValue', () => {
  it('needs to be tested')
})
